function numeros (){

    let a = 2
    let b = 3
    let c = 4

    let soma = a + c

    if ((a + c)> b) {
        console.log (`${a} + ${c} = ${soma}`)
        console.log(a + "+" + c + "=" + soma)
    }else{
        console.log(`${a} + ${c} < ou = ${b} `)
        console.log(a + "+" + c + ">=" + b)

    }

}
numeros()